# Instructions


## The link where it lies deployed.. awaiting someone to put a bug in it
`https://dev-and-dash.vercel.app/`


## Setting up the production:

1. clone the repository.

2. in root directory, run following commands:

```bash
npx live-server --port=3000
```

3. Edit the file, then send a pull request.

Happy coding
